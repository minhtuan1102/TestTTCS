using UnityEngine;

public class Hồi50máuchođồngđộitrong5s.Tốn50stamina : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
